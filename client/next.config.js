/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["images.unsplash.com", "cdn4.buysellads.net"],
  },
  // assetPrefix: "./",
};

module.exports = nextConfig;
